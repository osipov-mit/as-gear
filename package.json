{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "build": "lerna run asbuild:release",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^6.0.3"
  }
}
